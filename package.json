{
  "name": "sistema-comandas",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node backend/dist/server.js",
    "build": "cd backend && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "dependencies": {}
}
